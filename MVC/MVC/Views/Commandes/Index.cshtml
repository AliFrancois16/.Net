@model IEnumerable<DataBase.Commande>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.NomClient)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateCommande)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateLivraison)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StateDeCommande)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.NomClient)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateCommande)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateLivraison)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StateDeCommande)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCommande }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCommande }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCommande })
        </td>
    </tr>
}

</table>
