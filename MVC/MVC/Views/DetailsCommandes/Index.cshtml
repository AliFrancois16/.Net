@model IEnumerable<DataBase.DetailsCommande>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Commande.StateDeCommande)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Produit.NomProduit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prix)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantite)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Commande.StateDeCommande)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produit.NomProduit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prix)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantite)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDetailsCommandes }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDetailsCommandes }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDetailsCommandes })
        </td>
    </tr>
}

</table>
